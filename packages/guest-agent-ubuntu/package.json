{
  "name": "guest-agent-ubuntu",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ubuntu 24.04 sandbox guest agent: SSH agent + Docker bridges (systemd user units), env exports, agent rules.",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bridge-core": "workspace:*",
    "guest-rules": "workspace:*"
  }
}
